# Allow access to image files
<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Deny access to PHP files (if any)
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

